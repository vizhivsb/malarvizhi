#include<stdio.h>
int main()
{
	int a[5],i,b,c;
	printf("enter the no");
	scanf("%d",&a[i]);
	b=a[0];
	c=a[0];
	for(i=1;i<=4;i++)
	{
		printf("entr the no");
		scanf("%d",&a[i]);
		{
			if(a[i]>b)
			b=a[i];
			else
			b=0;
			
		}
	printf("the biggest no",b);
	return 0;
	}
}
